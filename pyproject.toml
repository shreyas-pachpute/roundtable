[project]
name = "roundtable-api"
version = "0.2.0"
requires-python = ">=3.12"
dependencies = [
  "anthropic>=1.5",
  "langgraph>=1.0",
  "fastapi>=0.117",
  "pydantic>=2.7",
  "pyyaml>=6.0",
  "httpx>=0.27",
  "psycopg[binary,pool]>=3.2",
  "langgraph-checkpoint-postgres>=3.0",
]

[tool.vercel]
entrypoint = "app:app"
